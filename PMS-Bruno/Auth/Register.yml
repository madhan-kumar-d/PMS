info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: "{{base}}/auth/register"
  body:
    type: json
    data: |-
      {
        "name": "Madhan",
        "role": "doctor"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
