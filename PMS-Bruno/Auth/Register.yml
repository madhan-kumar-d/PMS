info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: "{{base}}/register"
  body:
    type: json
    data: |-
      {
        "name": "Madhan"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
